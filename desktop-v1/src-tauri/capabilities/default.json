{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the SUP v1 shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
