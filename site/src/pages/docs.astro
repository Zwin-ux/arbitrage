---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Docs | Superior">
  <section class="page-hero compact">
    <p class="eyebrow">Docs</p>
    <h1>Build, install, and verify Superior from the same public repo.</h1>
    <p class="lead">
      The site is intentionally light. The real source of truth is still the monorepo, the desktop app, and the trust docs that explain paper-first mode and live gating.
    </p>
  </section>

  <section class="docs-grid">
    <article class="doc-card">
      <h2>Quick start</h2>
      <pre><code>python -m pip install -e ".[dev]"
market-data-recorder-app
market-data-recorder record</code></pre>
    </article>
    <article class="doc-card">
      <h2>Release build</h2>
      <pre><code>powershell -ExecutionPolicy Bypass -File scripts/bootstrap-iscc.ps1
powershell -ExecutionPolicy Bypass -File scripts/build-windows-release.ps1</code></pre>
    </article>
    <article class="doc-card">
      <h2>Quality gates</h2>
      <pre><code>pytest -q
python -m mypy src tests
npm --prefix site ci
npm --prefix site run build</code></pre>
    </article>
  </section>

  <section class="section-grid">
    <article class="feature-card">
      <p class="eyebrow">Key repo docs</p>
      <h3>Trust surface</h3>
      <p>Read the risk model, live-trading limitations, privacy/secrets notes, contributor strategy guide, release process, and third-party notices directly in the repo.</p>
    </article>
    <article class="feature-card">
      <p class="eyebrow">Product posture</p>
      <h3>Paper first, live gated</h3>
      <p>Superior is stable software with explicit guardrails, not a guarantee of profits. Users onboard, record local books, scan opportunities, and build paper history before any live-gate path appears.</p>
    </article>
    <article class="feature-card">
      <p class="eyebrow">Railway setup</p>
      <h3>Deploy only the site</h3>
      <p>Connect the GitHub repo to Railway, set the service root to <code>/site</code>, set the config path to <code>/site/railway.toml</code>, use the default Railway domain, and enable "Wait for CI". The app runtime, user data, and secrets stay off Railway.</p>
    </article>
  </section>
</BaseLayout>
